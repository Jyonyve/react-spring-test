import { inject, observer } from "mobx-react";

export const MemberEditFormContainer = inject("StoreLCycler")(observer(({}) => {
    
    function onSetClubProps(){
        
    }
    
    
    return(
        null
        //<MemberEditFormView/>
    );
    }        
))
